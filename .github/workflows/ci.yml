name: CI
on:
  push:
    branches: [ develop ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    environment:
      name: A
    steps:
      - name: Get Commit Message
        run: |
          echo ${{ secrets.TEST }}

      - name: Get
        uses: ./.github/workflows/ac.yml